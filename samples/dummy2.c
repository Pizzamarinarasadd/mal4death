/* Tutorial 2 sample: player opens this in PEview and reads
   the TimeDateStamp from IMAGE_FILE_HEADER. */
#include <windows.h>
#include <stdio.h>

int main(void) {
    printf("dummy2 - PE headers exercise\n");
    return 0;
}
